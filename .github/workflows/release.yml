name: Create Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build release APK
      run: ./gradlew assembleRelease --stacktrace
      
    - name: Get version info
      id: version
      run: |
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          VERSION="${{ github.event.inputs.tag }}"
        else
          VERSION=${GITHUB_REF#refs/tags/}
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "Building version: $VERSION"
        
    - name: Rename APK
      run: |
        mv app/build/outputs/apk/release/app-release-unsigned.apk \
           app/build/outputs/apk/release/exercise-tracker-${{ steps.version.outputs.version }}.apk
           
    - name: Generate changelog
      id: changelog
      run: |
        echo "## 🎉 运动记录应用 ${{ steps.version.outputs.version }}" > CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### ✨ 功能特性" >> CHANGELOG.md
        echo "- 📅 日历界面显示和导航" >> CHANGELOG.md
        echo "- 🏃‍♂️ 运动记录创建（20/30/40分钟选项）" >> CHANGELOG.md
        echo "- 💾 数据持久化存储" >> CHANGELOG.md
        echo "- 🔔 每日11:30运动提醒" >> CHANGELOG.md
        echo "- 🔒 记录不可变性保证" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### 📱 系统要求" >> CHANGELOG.md
        echo "- Android 7.0+ (API 24+)" >> CHANGELOG.md
        echo "- 存储空间: ~10MB" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### 📥 安装说明" >> CHANGELOG.md
        echo "1. 下载APK文件" >> CHANGELOG.md
        echo "2. 在Android设备上启用\"未知来源\"安装" >> CHANGELOG.md
        echo "3. 安装APK文件" >> CHANGELOG.md
        echo "4. 授予通知权限" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### 🔧 技术信息" >> CHANGELOG.md
        echo "- 开发语言: Kotlin" >> CHANGELOG.md
        echo "- 最小SDK: API 24 (Android 7.0)" >> CHANGELOG.md
        echo "- 目标SDK: API 34 (Android 14)" >> CHANGELOG.md
        echo "- 架构: MVC模式" >> CHANGELOG.md
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.version.outputs.version }}
        name: 运动记录应用 ${{ steps.version.outputs.version }}
        body_path: CHANGELOG.md
        files: |
          app/build/outputs/apk/release/exercise-tracker-${{ steps.version.outputs.version }}.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Upload APK to artifacts
      uses: actions/upload-artifact@v4
      with:
        name: exercise-tracker-release-${{ steps.version.outputs.version }}
        path: app/build/outputs/apk/release/exercise-tracker-${{ steps.version.outputs.version }}.apk
        retention-days: 90