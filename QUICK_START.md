# 🚀 快速开始 - GitHub Actions自动构建

## 📱 获取APK的最简单方法

### 第1步：上传到GitHub（5分钟）

1. **在GitHub创建新仓库**
   - 访问 https://github.com/new
   - 仓库名称：`exercise-tracker`（或其他名称）
   - 设为Public（这样Actions免费）
   - 点击"Create repository"

2. **运行自动设置脚本**
   ```powershell
   # 替换为你的仓库URL
   .\setup-github.ps1 -RepoUrl "https://github.com/你的用户名/exercise-tracker.git"
   ```

   或者手动执行：
   ```bash
   git init
   git add .
   git commit -m "Initial commit: 运动记录应用"
   git remote add origin https://github.com/你的用户名/exercise-tracker.git
   git branch -M main
   git push -u origin main
   ```

### 第2步：等待自动构建（3-5分钟）

1. **查看构建状态**
   - 访问你的GitHub仓库
   - 点击"Actions"标签页
   - 看到"Build Android APK"工作流正在运行

2. **等待完成**
   - 绿色✅表示构建成功
   - 红色❌表示构建失败（查看日志修复）

### 第3步：下载APK（1分钟）

1. **从Actions下载**
   - 点击完成的构建任务
   - 滚动到底部找到"Artifacts"
   - 下载"exercise-tracker-debug-apk"
   - 解压zip文件获得APK

2. **或创建正式发布**
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```
   - 然后在"Releases"页面下载APK

## 🎯 一键命令

如果你熟悉命令行，可以用这个一键脚本：

```powershell
# 设置你的GitHub用户名和仓库名
$username = "你的GitHub用户名"
$reponame = "exercise-tracker"

# 执行一键设置
git init
git add .
git commit -m "Initial commit: 运动记录应用完整项目"
git remote add origin "https://github.com/$username/$reponame.git"
git branch -M main
git push -u origin main

Write-Host "🎉 完成! 访问 https://github.com/$username/$reponame/actions 查看构建状态"
```

## 📋 构建内容

每次自动构建会生成：

- **Debug APK** - 用于测试，包含调试信息
- **Release APK** - 正式版本，体积更小
- **测试报告** - 单元测试和属性测试结果
- **构建日志** - 详细的构建过程记录

## 🔄 后续更新

每次修改代码后：

```bash
git add .
git commit -m "更新说明"
git push
```

GitHub Actions会自动：
- 运行所有测试
- 构建新的APK
- 上传到Artifacts供下载

## ⚡ 故障排除

### 构建失败？
1. 检查Actions页面的错误日志
2. 通常是网络问题，重新运行即可
3. 确保所有文件都已提交

### 找不到APK？
1. 确保构建显示绿色✅
2. 在构建详情页面底部找"Artifacts"
3. 下载的是zip文件，需要解压

### 权限问题？
1. 确保仓库是Public或有Actions权限
2. 检查Settings → Actions → General设置

## 🎉 完成！

现在你有了：
- ✅ 自动构建的Android APK
- ✅ 完整的CI/CD流水线  
- ✅ 自动化测试验证
- ✅ 一键发布功能

**只需要10分钟，就能获得专业级的Android应用构建流程！**